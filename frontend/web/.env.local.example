# Backend base URL. Leave empty for same-origin (reverse proxy in front of
# both the Next.js app and the FastAPI API). For a split local dev setup:
# NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_API_URL=
